import{addDoc,collection,doc,getDoc,getDocs,query,runTransaction,serverTimestamp,updateDoc,where}from'firebase/firestore';

export const DEPARTMENTS=[['customer-services','Customer Services'],['banking-operations','Banking Operations'],['lending-underwriting','Lending & Underwriting'],['collections-recovery','Collections & Recovery'],['business-commercial','Business & Commercial Services'],['fraud-investigations','Fraud & Investigations'],['risk-compliance','Risk & Compliance'],['internal-audit','Internal Audit'],['human-resources','Human Resources'],['training-development','Training & Development'],['technology-systems','Technology & Systems']].map(([id,name])=>({id,name}));
export const POSITIONS=[['customer-services','Customer Service Representative'],['banking-operations','Account Services Representative'],['lending-underwriting','Loan Processor'],['collections-recovery','Collections Representative'],['business-commercial','Business Account Specialist'],['fraud-investigations','Fraud Review Specialist'],['risk-compliance','Compliance Analyst'],['internal-audit','Junior Internal Auditor'],['human-resources','HR Assistant'],['training-development','Training Assistant'],['technology-systems','Technical Support Specialist']].map(([departmentId,name])=>({departmentId,name}));
const stamp=()=>new Date().toISOString();
const event=(status,label,actorType,actorUid)=>({status,label,actorType,actorUid,createdAt:stamp()});
const staffId=()=>`STF-${crypto.randomUUID().slice(0,8).toUpperCase()}`;

export async function getHiringAccess(db,uid){const[b,p,s]=await Promise.all([getDoc(doc(db,'system','bootstrap')),getDoc(doc(db,'staffPermissions',uid)),getDoc(doc(db,'staffProfiles',uid))]);const isOwner=b.exists()&&b.data().ownerUid===uid;const permissions=p.exists()?p.data().permissions||[]:[];return{isOwner,permissions,profile:s.exists()?s.data():null,mayReview:isOwner||permissions.some(x=>['staff.applications.view','staff.applications.review','staff.applications.decide','staff.manage','owner.override_all'].includes(x))};}
export async function loadStaffApplications(db,uid,all=false){const q=all?query(collection(db,'applications'),where('category','==','staff')):query(collection(db,'applications'),where('applicantUid','==',uid),where('category','==','staff'));const snap=await getDocs(q);return snap.docs.map(d=>({id:d.id,...d.data()})).sort((a,b)=>(b.updatedAt?.seconds||0)-(a.updatedAt?.seconds||0));}
export async function loadOrganization(db){const[b,d]=await Promise.all([getDocs(collection(db,'branches')),getDocs(collection(db,'departments'))]);return{branches:b.docs.map(x=>({id:x.id,...x.data()})).filter(x=>x.active!==false),departments:d.docs.map(x=>({id:x.id,...x.data()})).filter(x=>x.active!==false)};}
export async function createStaffApplication(db,userRecord,form){if(!form.departmentId||!form.position||form.motivation.trim().length<25)throw new Error('Complete the department, position, and motivation fields.');const responses={departmentId:form.departmentId,preferredBranchId:form.preferredBranchId||'capital',position:form.position,availability:form.availability.trim(),experience:form.experience.trim(),skills:form.skills.trim(),motivation:form.motivation.trim(),scenario:form.scenario.trim()};const ref=await addDoc(collection(db,'applications'),{applicationId:`APP-STF-${crypto.randomUUID().slice(0,8).toUpperCase()}`,applicantUid:userRecord.uid,customerId:userRecord.customerId,applicationType:'staff-application',applicationName:'Staff Application',category:'staff',status:'draft',currentStep:1,responses,applicantMessages:[],timeline:[event('draft','Staff application draft created','customer',userRecord.uid)],assignedReviewerUid:null,internalNotesCount:0,uploadsAllowed:false,createdAt:serverTimestamp(),updatedAt:serverTimestamp()});await updateDoc(ref,{status:'submitted',submittedAt:serverTimestamp(),updatedAt:serverTimestamp(),timeline:[event('draft','Staff application draft created','customer',userRecord.uid),event('submitted','Staff application submitted','customer',userRecord.uid)]});return ref.id;}
export async function advanceHiring(db,id,reviewer,action,data={}){const ref=doc(db,'applications',id);const snap=await getDoc(ref);if(!snap.exists())throw new Error('Application not found.');const app=snap.data();if(app.category!=='staff')throw new Error('This is not a staff application.');const labels={screen:'Initial screening completed',interview:'Interview requested',interviewed:'Interview completed',department:'Department review started',executive:'Executive review started',conditional:'Conditional offer issued',deny:'Application denied'};const statuses={screen:'initial-screening',interview:'interview-requested',interviewed:'interview-completed',department:'department-review',executive:'executive-review',conditional:'conditionally-accepted',deny:'denied'};const status=statuses[action];if(!status)throw new Error('Invalid hiring action.');const message=(data.message||'').trim();const update={status,assignedReviewerUid:reviewer.uid,assignedReviewerName:reviewer.name,updatedAt:serverTimestamp(),timeline:[...(app.timeline||[]),event(status,labels[action],'staff',reviewer.uid)],hiringReview:{score:Number(data.score||0),recommendation:data.recommendation||'',notes:(data.notes||'').trim(),reviewedBy:reviewer.uid,reviewedAt:stamp()}};if(message)update.applicantMessages=[...(app.applicantMessages||[]),{direction:'staff-to-customer',message,createdAt:stamp(),actorUid:reviewer.uid}];await updateDoc(ref,update);}
export async function appointStaff(db,id,reviewer,appointment){if((appointment.reason||'').trim().length<10)throw new Error('Enter an appointment reason.');const ref=doc(db,'applications',id);await runTransaction(db,async tx=>{const snap=await tx.get(ref);if(!snap.exists())throw new Error('Application not found.');const app=snap.data();if(app.category!=='staff'||['accepted','denied','withdrawn'].includes(app.status))throw new Error('This application cannot be appointed.');const uid=app.applicantUid;const userRef=doc(db,'users',uid),profileRef=doc(db,'staffProfiles',uid),permissionRef=doc(db,'staffPermissions',uid);const userSnap=await tx.get(userRef);if(!userSnap.exists())throw new Error('Applicant account not found.');const idValue=staffId();const departmentId=appointment.departmentId||app.responses?.departmentId;const branchId=appointment.branchId||app.responses?.preferredBranchId||'capital';const position=appointment.position||app.responses?.position||'Staff Trainee';const permissions=['staff.portal.access','academy.staff.view','policies.view','tasks.view'];tx.update(ref,{status:'accepted',appointment:{staffId:idValue,rankId:'rank-1',rankName:'Staff Trainee',departmentId,branchId,position,probation:true,appointedBy:reviewer.uid,appointedAt:stamp(),ownerOverride:Boolean(appointment.ownerOverride),reason:appointment.reason.trim()},updatedAt:serverTimestamp(),timeline:[...(app.timeline||[]),event('accepted','Appointed as Staff Trainee',reviewer.isOwner?'owner':'staff',reviewer.uid)]});tx.update(userRef,{staffId:idValue,primaryRole:'customer',roles:['customer','staff'],updatedAt:serverTimestamp()});tx.set(profileRef,{uid,staffId:idValue,customerId:app.customerId,rankId:'rank-1',rankName:'Staff Trainee',position,departmentId,branchId,staffStatus:'trainee',probationStatus:'active',protectedAccount:false,hiredAt:serverTimestamp(),appointedBy:reviewer.uid,applicationId:app.applicationId});tx.set(permissionRef,{uid,permissions,isOwner:false,globalOverride:false,updatedAt:serverTimestamp(),updatedBy:reviewer.uid});tx.set(doc(collection(db,'trainingAssignments')),{assignmentId:`TRN-${crypto.randomUUID().slice(0,8).toUpperCase()}`,staffUid:uid,staffId:idValue,type:'onboarding',courses:['afg-orientation','professional-conduct','customer-privacy','fictional-information-safety','conflict-of-interest'],status:'assigned',required:true,assignedBy:reviewer.uid,assignedAt:serverTimestamp()});tx.set(doc(collection(db,'staffAppointments')),{applicationId:app.applicationId,staffUid:uid,staffId:idValue,departmentId,branchId,position,rankId:'rank-1',ownerOverride:Boolean(appointment.ownerOverride),reason:appointment.reason.trim(),appointedBy:reviewer.uid,immutable:true,createdAt:serverTimestamp()});tx.set(doc(collection(db,'auditLogs')),{actorUid:reviewer.uid,actorType:appointment.ownerOverride?'owner':'staff',action:appointment.ownerOverride?'staff.appointment.overridden':'staff.appointment.created',targetType:'staff',targetId:idValue,reason:appointment.reason.trim(),immutable:true,createdAt:serverTimestamp()});});}
